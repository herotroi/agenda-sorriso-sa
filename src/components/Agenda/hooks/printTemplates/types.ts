
// Re-export from main types to avoid duplication
export type { Appointment, Professional, AppointmentStatus } from '@/types';

export type AppointmentDisplayType = 'start' | 'continuation' | 'end';
